# Flutter build artifacts
build/

# Dart tooling
.dart_tool/
.packages

# Rust build artifacts (core/rust)
target/

# Platform scaffolding — generated by `flutter create`; not tracked until then
android/
linux/

# Native libraries
*.so
*.dylib

# Flutter plugin generated files
.flutter-plugins
.flutter-plugins-dependencies

# Lockfile resolved separately for standalone core and app
pubspec.lock

# IDE / tooling metadata
.idea/

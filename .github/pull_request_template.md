<!--
  Thanks for the PR! The more context you give, the faster a maintainer can review.
  Keep each PR focused on ONE change. If AI-assisted, you must understand every line —
  per CONTRIBUTING.md, AI-generated code still needs human review.
-->

## What & Why

<!-- What does this change do, and what problem does it solve? Link the issue: fixes #123 -->

## How it was tested

<!-- Be specific: commands run and output, e2e/perf results, screenshots.
     CI runs: npm run tsc && npm run vitest run && npm run soak && npm run perf,
     flutter analyze && flutter test, dart analyze && dart test (core), cargo test (rust).
     If this touches the wire format or codec, note it — that's a BREAKING CHANGE. -->

- [ ] `npm run tsc`
- [ ] `npm run vitest run`
- [ ] `npm run soak`
- [ ] `flutter analyze` + `flutter test`
- [ ] Manually tested on: [Flutter Android / Flutter Linux / PWA / both]

## Checklist

- [ ] Closes an issue: `fixes #...`
- [ ] No wire-format/codec change, or a `BREAKING CHANGE` note added
- [ ] CHANGELOG / docs updated if user-facing
- [ ] If AI-assisted, I reviewed and understood every line

## Screenshots (if UI change)
